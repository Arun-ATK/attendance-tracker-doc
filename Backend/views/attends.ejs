<html>
<body>
<table class="attends-table" border=1 cell-spacing=0>
  <thead>
    <tr>
      <th>Student id</th>
      <th>Lecture id</th>
      <th>Is Prsent</th>
      <th>Duration</th>
    </tr>
  </thead>

  <tbody>
    <% for(var i=0; i < faculty.length; i++) { %>
    <tr>
     <td><%= faculty[i].student_id %></td>
     <td><%= faculty[i].lecture_id %></td>
     <td><%= faculty[i].ispresent %></td>
     <td><%= faculty[i].duration %></td>
    </tr>
    <% } %>
  </tbody>
  </table>
  <style>
  body{
       font-size : 25px;
       background:linear-gradient(#07F5F5,white);
       position:absolute;
       top:50%;
       left:50%;
       transform: translate(-50%,-50%);
       text-align:center;
  }
 </style>
</body>
</html>
